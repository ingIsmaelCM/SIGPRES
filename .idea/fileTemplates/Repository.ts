import { BaseRepository } from "@/app/repositories/BaseRepository";
import { ${MODEL_NAME} } from "@source/models";

export default class ${MODEL_NAME}Repository extends BaseRepository<${MODEL_NAME}> {
  constructor() {
    super(${MODEL_NAME});
  }

}
