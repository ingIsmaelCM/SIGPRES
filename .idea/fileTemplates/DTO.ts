import {I${MODEL_NAME}} from "@app/interfaces/SourceInterfaces";
import BaseDTO from "@app/dto/BaseDTO";

export default class ${MODEL_NAME}DTO extends  BaseDTO implements  I${MODEL_NAME}{
    constructor(data: ${MODEL_NAME}DTO) {
        super();
        
    }


}